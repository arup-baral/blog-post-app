/* eslint-disable react/prop-types */

function Logo({width=60, height=60}) {
  return (
    <div style={{
        width: width,
        height: height,
    }}>
        Logo
    </div>
  )
}

export default Logo;